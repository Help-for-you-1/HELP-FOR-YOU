/* HELP FOR YOU — EMI action button fix
   Only fixes EMI action buttons. Existing Admin layout/options stay unchanged.
*/
(function(){
'use strict';
function sb(){return window.supabase.createClient(window.HFY_SUPABASE_URL,window.HFY_SUPABASE_PUBLISHABLE_KEY);}
function money(v){return Number(v||0).toFixed(2);}
function esc(v){return String(v==null?'':v).replace(/[&<>\"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c];});}
async function getEmi(id){var r=await sb().from('loan_emi_schedule').select('*').eq('id',id).maybeSingle();if(r.error)throw r.error;if(!r.data)throw new Error('EMI not found');return r.data;}
async function refresh(){if(window.loadData)await window.loadData();}
function today(){return new Date().toISOString().slice(0,10);}
window.hfyPay=async function(id){try{var e=await getEmi(id);if(String(e.status||'').toLowerCase()==='paid'){alert('This EMI is already paid.');return;}var defaultAmount=Number(e.emi_amount||0)+Number(e.penalty||0)-Number(e.paid_amount||0);if(defaultAmount<=0)defaultAmount=Number(e.emi_amount||0)||0;openBox('Payment Now','<div class="form">'+'<label>Amount<input id="hfPayAmount" type="number" min="0.01" step="0.01" value="'+money(defaultAmount)+'" inputmode="decimal"></label>'+'<label>Payment Date<input id="hfPayDate" type="date" value="'+today()+'"></label>'+'<div class="full"><button class="btn blue" onclick="hfySubmitPayment(\''+esc(id)+'\')">Pay Now</button></div>'+'</div>');}catch(e){alert('Payment load failed: '+(e.message||e));}};
window.hfySubmitPayment=async function(id){try{var amount=Number(document.getElementById('hfPayAmount').value||0),paymentDate=document.getElementById('hfPayDate').value;if(amount<=0)throw new Error('Payment amount must be greater than 0');if(!paymentDate)throw new Error('Payment Date is required');var r=await sb().rpc('hfy_record_emi_payment_allocated',{p_emi_id:id,p_amount:amount,p_payment_date:paymentDate,p_payment_type:'EMI + Overdue Payment',p_remarks:'Manual payment; allocated to applicable EMI dates'});if(r.error)throw r.error;if(window.closeM)window.closeM();await refresh();var d=r.data||{};alert('Payment recorded successfully. Allocated ₹'+money(d.allocated_amount||amount)+(Number(d.unallocated_amount||0)>0?' | Unallocated ₹'+money(d.unallocated_amount):''));}catch(e){alert('Payment update failed: '+(e.message||e));}};
window.pay=window.hfyPay;
window.hfyEditEmi=async function(id){try{var e=await getEmi(id);var status=String(e.status||'upcoming').toLowerCase();if(status==='pending')status='upcoming';openBox('Edit EMI','<div class="form">'+'<label>EMI Number<input value="'+esc(e.emi_number)+'" readonly></label>'+'<label>Due Date<input id="hfDue" type="date" value="'+esc(String(e.due_date||'').slice(0,10))+'"></label>'+'<label>EMI Amount<input id="hfAmt" type="number" step="0.01" value="'+Number(e.emi_amount||0)+'"></label>'+'<label>Penalty<input id="hfPen" type="number" step="0.01" value="'+Number(e.penalty||0)+'"></label>'+'<label>Paid Amount<input id="hfPaid" type="number" step="0.01" value="'+Number(e.paid_amount||0)+'"></label>'+'<label>Status<select id="hfStatus">'+'<option value="upcoming" '+(status==='upcoming'?'selected':'')+'>Pending</option>'+'<option value="overdue" '+(status==='overdue'?'selected':'')+'>Overdue</option>'+'<option value="paid" '+(status==='paid'?'selected':'')+'>Paid</option>'+'</select></label>'+'<div class="full"><button class="btn blue" onclick="hfySaveEmi(\''+esc(id)+'\')">Save Changes</button></div>'+'</div>');}catch(e){alert('EMI edit load failed: '+(e.message||e));}};
window.editEmi=window.hfyEditEmi;
window.hfySaveEmi=async function(id){try{var amount=Number(document.getElementById('hfAmt').value||0),penalty=Number(document.getElementById('hfPen').value||0),paid=Number(document.getElementById('hfPaid').value||0),due=document.getElementById('hfDue').value,status=document.getElementById('hfStatus').value;if(amount<=0)throw new Error('EMI amount must be greater than 0');if(!due)throw new Error('Due Date is required');var total=amount+penalty;if(status==='paid')paid=total;paid=Math.min(Math.max(paid,0),total);var r=await sb().from('loan_emi_schedule').update({due_date:due,emi_amount:amount,penalty:penalty,total_due:total,paid_amount:paid,remaining_amount:Math.max(0,total-paid),status:status}).eq('id',id);if(r.error)throw r.error;if(window.closeM)window.closeM();await refresh();alert('EMI updated successfully');}catch(e){alert('EMI update failed: '+(e.message||e));}};
window.updateEmi=window.hfySaveEmi;
setTimeout(function(){window.pay=window.hfyPay;window.editEmi=window.hfyEditEmi;window.updateEmi=window.hfySaveEmi;},2500);
})();
